import { styled } from "@mui/system";

const ImportResumeStyled = styled("div")(() => ({
  display: "flex",
  flexFlow: "column",
  alignItems: "center",
  justifyContent: "center",
  padding: "10px",
  label: {
    backgroundColor: "#fff",
  },
}));

export default ImportResumeStyled;
